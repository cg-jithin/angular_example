pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'ls'
                // sh 'docker build -t angular-app .'
            }
        }
    }
}